do_up(i,a) {
  if i = a.length then
    ()
   else {
     a[i] <- i;
	 do_up((i + 1),a)
   }
}

check_up(i,a) {
  if i = a.length then
    ()
  else {
    assert(a[i] = 0);
	check_up((i + 1),a)
  }
}

{
  let x = mkarray 7 in {
    do_up(0,x);
	check_up(0,x)
  }
}
