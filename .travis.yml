language: c
addons:
  apt:
    sources:
      - avsm
    packages:
      - opam
      - ocaml
      - libstdc++6
      - libgomp1
      - libgmp-dev
      - python
sudo: false
cache:
  directories:
    - $HOME/.opam
    - $HOME/jdk8
script: bash -ex .travis-ci.sh
